
kind: Service
apiVersion: v1
metadata:
  name: loadbalancer-service
  spec: 
    healthCheckNodePort: 8000
    ports: 
    - name: "8000"
      port: 8000
      targetPort: 8000
    selector:
      app: loadbalancer
    type: LoadBalancer
    externalTrafficPolicy: Local